# NOTE: LEGACY EXAMPLE MANIFEST
# Prefer Helm ingress template with TLS configuration via cert-manager.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: doganai-compliance-kit
  namespace: doganai-compliance
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: doganai-compliance.doganai.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: doganai-compliance-kit
                port: { number: 80 }
